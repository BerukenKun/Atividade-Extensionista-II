function changeColor(element) {
    element.style.color = 'magenta'; // Altera a cor para magenta quando passar o mouse
}

function resetColor(element) {
    element.style.color = ''; // Resetar para a cor padrão
}
